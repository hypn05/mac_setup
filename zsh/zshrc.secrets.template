# ~/.zshrc.secrets — copied here once by modules/zsh.sh (chmod 600), then yours to edit.
# Long-lived secrets and tokens, sourced from ~/.zshrc. Never committed to git,
# never symlinked back to the repo — this file is machine-local by design.

# export GITHUB_TOKEN="..."
# export OPENAI_API_KEY="..."
# export ANTHROPIC_API_KEY="..."
